{
  "name": "@kiado-monorepo",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "babel-plugin-react-compiler": "^1.0.0",
    "turbo": "^2.8.9"
  },
  "packageManager": "npm@11.3.0"
}
