{
  "name": "@kiado-monorepo",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "packageManager": "pnpm@10.30.1",
  "devDependencies": {
    "turbo": "^2.8.9"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "~19.1.10",
      "@types/react-dom": "~19.1.5"
    }
  }
}
